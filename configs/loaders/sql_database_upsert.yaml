# configs/loaders/sql_database_upsert.yaml
# SQLite database loader with upsert (INSERT ... ON CONFLICT DO UPDATE).

connection_string: "sqlite:///output/todos.db"
table_name: "todos"
if_exists: "upsert"
primary_keys: ["id"]
index: false
